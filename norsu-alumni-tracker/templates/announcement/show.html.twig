{% extends 'base.html.twig' %}

{% block title %}{{ announcement.title }}{% endblock %}

{% block body %}
<div class="flex justify-between items-center mb-3">
    <a href="{{ path('announcement_index') }}" class="btn-outline-norsu btn-sm no-print"><i class="bi bi-arrow-left mr-1"></i>Back</a>
    <div class="flex items-center gap-2 no-print">
        <button onclick="window.print()" class="inline-flex items-center gap-1 bg-gray-600 hover:bg-gray-700 text-white text-sm font-semibold px-3 py-2 rounded-lg transition">
            <i class="bi bi-printer"></i>Print
        </button>
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('announcement_edit', {id: announcement.id}) }}" class="btn-norsu btn-sm"><i class="bi bi-pencil mr-1"></i>Edit</a>
        {% endif %}
    </div>
</div>

{# ── Print-only header ── #}
<div class="hidden print-header" style="display:none">
    <div style="text-align:center; margin-bottom:20px;">
        <h2 style="margin:0; font-size:18pt; font-weight:bold;">Negros Oriental State University</h2>
        <h3 style="margin:4px 0; font-size:14pt;">Announcement</h3>
        <hr style="margin-top:10px;">
    </div>
</div>

<div class="card">
    <div class="card-body">
        <div class="mb-3">
            {% if announcement.category %}
                <span class="badge bg-gray-100 text-gray-700">{{ announcement.category }}</span>
            {% endif %}
            {% if not announcement.isActive %}
                <span class="badge badge-secondary">Draft</span>
            {% endif %}
            <span class="text-gray-500 text-sm ml-2">{{ announcement.datePosted|date('F d, Y \\a\\t h:i A') }}</span>
        </div>
        <h3 class="font-bold mb-3">{{ announcement.title }}</h3>
        {% if announcement.eventStartAt or announcement.location %}
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 mb-4">
                <div class="rounded-lg bg-blue-50 border border-blue-100 px-3 py-3">
                    <p class="text-xs uppercase tracking-wide text-blue-700 font-bold mb-1">Date</p>
                    <p class="text-sm font-semibold text-gray-800 mb-0">{{ announcement.eventStartAt ? announcement.eventStartAt|date('M d, Y') : 'Date TBA' }}</p>
                </div>
                <div class="rounded-lg bg-blue-50 border border-blue-100 px-3 py-3">
                    <p class="text-xs uppercase tracking-wide text-blue-700 font-bold mb-1">Time</p>
                    <p class="text-sm font-semibold text-gray-800 mb-0">{{ announcement.eventStartAt ? announcement.eventStartAt|date('h:i A') : 'Time TBA' }}</p>
                </div>
                <div class="rounded-lg bg-blue-50 border border-blue-100 px-3 py-3">
                    <p class="text-xs uppercase tracking-wide text-blue-700 font-bold mb-1">Location</p>
                    <p class="text-sm font-semibold text-gray-800 mb-0">{{ announcement.location ?: 'Location TBA' }}</p>
                </div>
            </div>
        {% endif %}
        <div class="mb-3" style="white-space: pre-line;">{{ announcement.description }}</div>
        {% if announcement.joinUrl %}
            <div class="mb-3 no-print">
                <a href="{{ announcement.joinUrl }}" target="_blank" rel="noopener noreferrer" class="btn-norsu btn-sm"><i class="bi bi-link-45deg mr-1"></i>Open Link</a>
            </div>
        {% endif %}
        {% if announcement.postedBy %}
            <hr class="my-3 border-gray-200">
            <span class="text-gray-500 text-sm">Posted by <strong>{{ announcement.postedBy.fullName }}</strong></span>
        {% endif %}
    </div>
</div>
{% endblock %}
